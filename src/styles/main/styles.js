import {lightGrayColor} from "../global";

export const spinnerColor = '#fc327a';

export const MAIN_STYLES = {
    main: {
        flex: 1,
        flexDirection: "row",
        backgroundColor: lightGrayColor,
        alignItems: "center",
        justifyContent: "center"
    },
};